# 基于两阶段深度学习的腰椎间盘突出自动分割方法研究

## 1. 研究现状以及发展态势

### 1.1 医学影像分割技术发展

医学影像自动分割技术是计算机辅助诊断（CAD）系统的核心组成部分。随着深度学习技术的快速发展，特别是卷积神经网络（CNN）和U-Net架构的提出，医学影像分割领域取得了显著进展。nnU-Net作为自配置的医学图像分割框架，能够根据数据集特性自动调整网络架构和训练参数，在多个医学影像分割挑战赛中取得了优异的成绩，已成为医学影像分割领域的标杆方法。

### 1.2 脊柱影像分析研究现状

脊柱影像分析是医学影像处理的重要分支，主要任务包括椎骨、椎间盘、脊髓和椎管等结构的自动分割与识别。TotalSpineSeg作为近年来提出的脊柱自动分割工具，采用两阶段混合架构（Step1粗分割 + Step2精分割），结合nnU-Net深度学习模型和迭代后处理算法，实现了对MRI图像中所有椎骨、椎间盘、脊髓和椎管的自动实例分割，对各种MRI对比度、采集方向和分辨率具有良好的鲁棒性。

### 1.3 腰椎间盘突出分割研究现状

腰椎间盘突出（Lumbar Disc Herniation, LDH）是临床常见的脊柱疾病，其准确分割对于疾病诊断、手术规划和疗效评估具有重要意义。然而，LDH分割面临以下挑战：（1）LDH病灶体积小，通常仅占椎间盘体积的5-10%，存在严重的类别不平衡问题；（2）LDH边界模糊，与正常椎间盘组织区分困难；（3）不同椎间盘位置的LDH形态差异大，需要位置先验信息；（4）传统单阶段分割方法难以同时兼顾检测召回率和分割精度。

目前，针对LDH分割的研究相对较少，多数方法采用单阶段nnU-Net直接分割，存在假阳性率高、边界精度不足等问题。两阶段检测-分割架构在自然图像目标检测领域已得到广泛应用，但在医学影像小目标分割中的应用仍处于探索阶段。

### 1.4 发展趋势

未来医学影像分割技术的发展趋势包括：（1）多阶段级联架构，通过检测-分割分离策略提升小目标分割性能；（2）解剖学先验知识的融合，利用医学领域的专业知识指导网络学习；（3）边界感知损失函数，通过距离变换和边界损失优化分割边界精度；（4）自监督和弱监督学习，减少对大量标注数据的依赖。

## 2. 选题依据以及意义

### 2.1 选题依据

腰椎间盘突出是临床最常见的脊柱退行性疾病之一，准确、快速的LDH自动分割对于提高诊断效率、辅助手术规划和客观评估治疗效果具有重要临床价值。然而，现有的单阶段分割方法在处理小目标、类别不平衡和边界精度方面存在明显不足。

本项目基于TotalSpineSeg框架，提出了一种两阶段LDH分割方法，通过检测-分割分离策略，结合解剖学先验知识和表面感知损失函数，旨在解决LDH分割中的关键技术难题。该研究具有明确的理论意义和实际应用价值。

### 2.2 研究意义

**理论意义**：（1）探索两阶段架构在医学影像小目标分割中的应用，验证检测-分割分离策略的有效性；（2）研究解剖学先验知识（椎间盘索引图）对分割性能的提升作用；（3）探索表面感知损失函数（Signed Distance Map + Boundary Loss）在边界精度优化中的机制。

**实际意义**：（1）为临床提供高精度的LDH自动分割工具，提高诊断效率和准确性；（2）为手术规划提供精确的病灶定位和量化信息；（3）为疾病进展监测和疗效评估提供客观的量化指标；（4）为相关医学影像分割任务提供可借鉴的技术方案。

## 3. 课题研究内容

### 3.1 两阶段LDH分割架构设计

本研究采用两阶段级联架构，将LDH分割任务分解为检测和分割两个子任务：

**Stage A（检测阶段）**：采用3D CNN网络（5个卷积块 + 全局平均池化 + 全连接层），输入为3通道（MRI图像 + 椎间盘掩码 + 椎间盘索引图），输出为二分类结果（是否存在LDH）。该阶段专注于优化召回率，确保不遗漏LDH病灶。

**Stage B（分割阶段）**：采用小型3D U-Net网络（4层，通道数16→128），输入同样为3通道，输出为LDH二值掩码。该阶段仅在Stage A检测为阳性的椎间盘区域进行精细分割，专注于优化分割精度和边界准确性。

### 3.2 解剖学先验知识融合

基于TotalSpineSeg的Step2输出（包含完整椎骨和椎间盘标注），生成归一化椎间盘索引图（Disc Index Map）。该索引图将不同椎间盘位置（L1/L2到L5/S1）编码为连续归一化值（0-1），作为空间先验信息输入网络，帮助模型区分不同位置的椎间盘，提升分割性能。

### 3.3 强制四类采样策略

针对LDH样本不平衡问题，提出强制四类采样策略：（1）LDH中心点采样（正例）；（2）LDH边界点采样（正例）；（3）椎间盘边界负样本采样（负例，必须）；（4）椎间盘内部负样本采样（负例，必须）。该策略确保训练样本覆盖所有关键场景，避免模型过拟合单一样本类型。

### 3.4 表面感知损失函数设计

Stage B采用组合损失函数：FocalTversky Loss + Boundary Loss + L1(Signed Distance Map)。其中，FocalTversky Loss解决类别不平衡问题；Boundary Loss基于距离变换直接优化边界IoU；L1(SDM)回归真实符号距离图，强制网络学习表面几何信息，提升边界分割精度。

### 3.5 数据准备与评估体系

设计完整的数据准备流程，包括Step1/Step2推理、椎间盘索引图生成、Stage A补丁提取（96³）和Stage B ROI提取（48³）。建立多指标评估体系，包括椎间盘级召回率、病灶级检出率、Dice系数和平均表面距离（ASD），全面评估模型性能。

## 4. 拟解决的关键问题和最终目标，以及拟采取的主要理论、技术路线和实施方案

### 4.1 拟解决的关键问题

**问题1：小目标分割的召回率与精度平衡**。LDH病灶体积小，单阶段方法难以同时兼顾高召回率和高精度。**解决方案**：采用两阶段架构，Stage A专注于检测（优化召回率），Stage B专注于分割（优化精度），实现任务解耦。

**问题2：严重的类别不平衡**。LDH仅占椎间盘体积的5-10%，随机采样会导致负样本过拟合。**解决方案**：强制四类采样策略，确保正负样本平衡，并显式采样边界点。

**问题3：边界分割精度不足**。传统Dice Loss对小位移不敏感，导致边界模糊。**解决方案**：引入Boundary Loss和Signed Distance Map监督，提供连续梯度信号，直接优化边界精度。

**问题4：缺乏空间位置先验**。不同椎间盘位置的LDH形态差异大，需要位置信息指导。**解决方案**：基于Step2标注生成椎间盘索引图，作为空间先验输入网络。

### 4.2 最终目标

（1）**性能目标**：椎间盘级召回率≥95%，病灶级检出率≥90%，Dice系数≥0.75，平均表面距离≤2.0mm。

（2）**技术目标**：建立完整的两阶段LDH分割流水线，包括数据准备、模型训练、推理和评估模块，形成可复现的技术方案。

（3）**应用目标**：为临床提供高精度的LDH自动分割工具，支持疾病诊断、手术规划和疗效评估。

### 4.3 技术路线

**第一阶段：数据准备与预处理**
- 利用TotalSpineSeg进行Step1和Step2推理，获得完整脊柱分割结果
- 生成椎间盘索引图，将不同椎间盘位置编码为归一化值
- 提取Stage A训练补丁（96³）和Stage B训练ROI（48³）
- 实施强制四类采样，确保训练样本多样性

**第二阶段：Stage A检测模型训练**
- 设计3D CNN检测网络（输入3通道，输出二分类）
- 使用Focal Loss（γ=2）优化召回率
- 训练约50个epoch，使用AdamW优化器和余弦退火学习率调度
- 评估椎间盘级召回率和病灶级检出率

**第三阶段：Stage B分割模型训练**
- 设计小型3D U-Net分割网络（4层，16→128通道）
- 使用组合损失函数（FocalTversky + Boundary + L1(SDM)）
- 仅在Stage A检测为阳性的ROI上训练
- 训练约200个epoch，使用AdamW优化器和ReduceLROnPlateau学习率调度
- 评估Dice系数和平均表面距离

**第四阶段：联合推理与评估**
- 实现端到端推理流水线：Step1/Step2 → 椎间盘索引图 → Stage A检测 → Stage B分割
- 聚合所有椎间盘级预测，生成最终LDH掩码
- 使用多指标评估体系全面评估模型性能
- 进行消融实验，验证各技术组件的有效性

### 4.4 实施方案

**开发环境**：Python 3.10+，PyTorch，nnU-Net v2，CUDA GPU（≥16GB显存）

**数据集**：使用TotalSpineSeg LDH数据集（Dataset 105），包含约400例LDH病例，分为训练集和测试集。

**训练策略**：
- Stage A：批量大小32，初始学习率1e-3，训练50个epoch
- Stage B：批量大小16，初始学习率1e-4，训练200个epoch
- 使用数据增强（旋转、缩放、弹性变形等，但不使用镜像翻转以保持解剖正确性）

**评估方法**：
- 使用5折交叉验证评估模型稳定性
- 在独立测试集上评估最终性能
- 进行消融实验分析各技术组件的贡献

**预期时间安排**：
- 数据准备：1-2周
- Stage A训练与调优：1-2周
- Stage B训练与调优：2-3周
- 联合评估与论文撰写：2-3周

## 5. 论文特色或创新点

### 5.1 两阶段检测-分割架构

首次将两阶段检测-分割架构应用于LDH分割任务，通过任务解耦实现召回率与精度的平衡。Stage A专注于检测，优化召回率；Stage B专注于分割，优化精度。相比单阶段方法，该架构能够显著提升小目标分割性能。

### 5.2 解剖学先验知识融合

创新性地提出基于Step2标注的椎间盘索引图，将不同椎间盘位置编码为连续归一化值，作为空间先验信息输入网络。该先验知识帮助模型区分不同位置的椎间盘，提升分割性能，特别是在处理多节段LDH时。

### 5.3 强制四类采样策略

针对LDH样本不平衡问题，提出强制四类采样策略，确保训练样本覆盖LDH中心、LDH边界、椎间盘边界和椎间盘内部等关键场景。该策略避免了随机采样导致的负样本过拟合问题，提升了模型对边界区域的判别能力。

### 5.4 表面感知损失函数

设计组合损失函数，融合FocalTversky Loss、Boundary Loss和Signed Distance Map回归损失。其中，Boundary Loss基于距离变换直接优化边界IoU，SDM回归损失强制网络学习表面几何信息，显著提升了边界分割精度。

### 5.5 完整的端到端流水线

建立了从数据准备、模型训练、推理到评估的完整端到端流水线，所有模块均基于TotalSpineSeg框架实现，具有良好的可复现性和可扩展性。该流水线可直接应用于临床，为LDH自动分割提供完整的解决方案。

---

**总结**：本研究针对LDH分割中的关键技术难题，提出了一种基于两阶段深度学习的自动分割方法。通过检测-分割分离策略、解剖学先验知识融合、强制采样策略和表面感知损失函数，实现了高精度的LDH分割。该研究具有重要的理论意义和实际应用价值，为医学影像小目标分割提供了新的技术思路。

